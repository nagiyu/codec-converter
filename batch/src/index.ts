#!/usr/bin/env node
/**
 * Batch worker entry point
 *
 * This is the main entry point for the batch worker.
 * It delegates to the worker module for video codec conversion.
 */

// Import and execute the worker module
import './worker';
